extends ../layout

block content
  .mx-auto.container
    h2 #{title}
    strong Are you sure you want to delete #{player.name}
    form(method="POST")
      button(type="submit" class="px-3 py-2 rounded bg-red-700 text-white") Delete #{player.name}
