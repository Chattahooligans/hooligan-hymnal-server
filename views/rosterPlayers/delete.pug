extends ../layout

block content
  h2 #{title}
  strong Are you sure you want to remove #{player.hint} from #{roster.rosterTitle}
  form(method="POST")
    button(type="submit" class="px-3 py-2 rounded bg-red-700 text-white") Remove Player
