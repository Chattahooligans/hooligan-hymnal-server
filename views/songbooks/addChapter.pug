extends ../layout

include ../mixins/_baseInput

block content
  .mx-auto.container
    h2 #{title}
    form(method="POST")
      .flex.flex-col.mb-3
        +baseInput("text", "Chapter Title", "chapter_title", "", "Chapter Title", true)
      .mb-3
        button(type="submit" class="px-3 py-2 rounded bg-blue-700 text-white") Save Chapter
