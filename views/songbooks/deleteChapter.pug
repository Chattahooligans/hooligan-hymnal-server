extends ../layout

include ../mixins/_csrfInput

block content
  .mx-auto.container
    h2 #{title}
    form(method="POST")
      +csrfInput()
      button(type="submit" class="px-3 py-2 rounded bg-red-700 text-white") Delete #{chapter.chapter_title}
