extends ../layout

include ../mixins/_csrfInput

block content
  .mx-auto.container
    h2 #{title}
    strong(class="block") Are you sure you want to delete #{foe.opponent}
    form(method="POST")
      +csrfInput()
      button(type="submit" class="px-3 py-2 rounded bg-red-700 text-white") Delete
